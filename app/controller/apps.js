module.exports = {
   async create() {
        const app = await App.create(req.body)
    },

    update(id, app) {
        //Todo update
    },

    find(id, app) {
        //Todo find
    },

    query(params) {
        //Todo query
    },

    delete(id) {
        //Todo delete
    }
}